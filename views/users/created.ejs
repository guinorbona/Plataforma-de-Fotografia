<%- include('../partials/header') %>
    <%- include('../partials/navbar') %>

        <main class="container">
            <div class="form-card">
                <h1>Usuário criado com sucesso</h1>
                <p>O usuário foi cadastrado com as seguintes informações:</p>
                <ul style="list-style:none; padding-left:0; margin-top:0.75rem; margin-bottom:1rem; font-size:0.9rem;">
                    <li><strong>Nome:</strong>
                        <%= user.name %>
                    </li>
                    <li><strong>E-mail:</strong>
                        <%= user.email %>
                    </li>
                    <li><strong>Perfil:</strong>
                        <%= user.role %>
                    </li>
                </ul>
                <div
                    style="background:#020617; border-radius:8px; padding:0.75rem 1rem; border:1px dashed rgba(148,163,184,0.7); margin-bottom:1rem;">
                    <p style="margin:0 0 0.25rem 0; font-size:0.9rem;">
                        <strong>Senha temporária:</strong>
                    </p>
                    <div class="password-wrapper" style="max-width:350px;">
                        <input type="password" value="<%= tempPassword %>" readonly style="font-family:monospace;" />
                        <span class="material-icons password-toggle">visibility</span>
                    </div>
                    <p style="margin-top:0.5rem; font-size:0.8rem; color:#9ca3af;">
                        Compartilhe esta senha com o usuário. Ele será obrigado a alterá-la no primeiro login.
                    </p>
                </div>
                <div style="display:flex; gap:0.5rem; flex-wrap:wrap;">
                    <a href="/admin/usuarios" class="btn btn-primary btn-small">Ir para lista de usuários</a>
                    <a href="/admin/usuarios/novo" class="btn btn-outline btn-small">Cadastrar outro usuário</a>
                </div>
            </div>
        </main>

        <%- include('../partials/footer') %>